org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
# JDK do sistema (26, "Newer than known valid Java version") quebra o jlink
# usado no jdkImage transform do AGP; usamos o OpenJDK 17 (LTS) instalado via
# `brew install openjdk@17` só para o build Android deste projeto.
org.gradle.java.home=/usr/local/opt/openjdk@17
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
